spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: 账号
    password: 密码
    url: jdbc:mysql://localhost:3306/lchat-server?serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
    ###########【Kafka集群】###########
  kafka:
    bootstrap-servers: Kafka服务
    consumer:
      group-id: defaultGroupId
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

server:
  port: 8077

endpoint:
accessKeyId:
accessKeySecret:
bucket:
