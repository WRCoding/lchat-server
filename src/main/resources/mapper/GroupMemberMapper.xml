<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lpepsi.lchatserver.dao.mapper.GroupMemberMapper">
    <select id="findGroupIdByLcid" resultType="string">
        select group_id from lchat_group_member where group_member = #{lcid};
    </select>
</mapper>